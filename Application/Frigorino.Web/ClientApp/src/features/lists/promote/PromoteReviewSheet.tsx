import type React from "react";

interface PromoteReviewSheetProps {
    open: boolean;
    onClose: () => void;
    householdId: number;
    listId: number;
}

// Stub — full implementation in Task 9.
export const PromoteReviewSheet: React.FC<PromoteReviewSheetProps> = () =>
    null;
